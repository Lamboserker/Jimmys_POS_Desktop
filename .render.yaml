services:
  - type: web
    name: jimmys-pos-dashboard
    env: static
    buildCommand: npm run build
    startCommand: npm run preview
    staticPublishPath: ./dist
    envVars:
      - key: NODE_ENV
        value: production
